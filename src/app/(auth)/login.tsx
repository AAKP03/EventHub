// src/app/(auth)/login.tsx  (NEW FILE)
//
// Expo Router turns this file's path into the URL "/login".
// It just renders the real screen component from src/screens.

import LoginScreen from "../../screens/LoginScreen";

export default function Login() {
  return <LoginScreen />;
}
